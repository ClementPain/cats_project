<%= form_with(model: line_cat_picture, local: true) do |form| %>
  <% if line_cat_picture.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(line_cat_picture.errors.count, "error") %> prohibited this line_cat_picture from being saved:</h2>

      <ul>
      <% line_cat_picture.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cart_id %>
    <%= form.text_field :cart_id %>
  </div>

  <div class="field">
    <%= form.label :cat_picture_id %>
    <%= form.text_field :cat_picture_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
