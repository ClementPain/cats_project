<div id="page-container">
<div class="banner">
        <div class="ban">
          <h1 class="textban" style="font-size:100px">Cats Project</h1>
          <p>Achate Meow Souris Photos!</p>

        </div>
      </div>

<div class="container">
  <div class="row">
    
 	<% @cat_pictures.each do |item|%>
	    
	   <div class="col-12 col-sm-8 col-md-6 col-lg-4 mt-4">
	    <div class="card">
			<%= image_tag item.image_url, size: '300x300', style: "border-radius: 30px;" %>
			  <h4 class="card-title"><%= item.title %></h4>
			  <p class="card-price"><%= item.price%>€</p>
        <%= link_to 'Voir le produit', cat_picture_path(item.id), class: 'btn btn-secondary' %>
			  

			</div>
		</div>
		<% end %>	    
   </div>
</div>
  
</div>