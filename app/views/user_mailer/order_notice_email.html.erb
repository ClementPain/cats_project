<!DOCTYPE html>
<html>
  <head>
    <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  </head>
  <body>
    <h2>Récapitulatif de la commande n°<%= @order.id %></h2>
    
    <p>Utilisateur : <%= @user.email %></p>
    <p>Montant : <%= @order.cat_pictures.sum(:price) %> €</p>
    </br>
    <% @order.cat_pictures.each do |item| %>
        *<%= item.title %>
    <%end%>
  </body>
</html>